# 环球汇通外汇交易平台 - 前端技术栈规范

## 1. 技术选型总览

### 1.1 交易终端（Web - React）

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心框架 | React | 18.x | UI框架 |
| 语言 | TypeScript | 5.x | 类型安全 |
| UI组件库 | Ant Design Pro | 5.x | 企业级组件库 |
| 构建工具 | Vite | 5.x | 快速构建 |
| 状态管理 | Zustand | 4.x | 轻量级状态管理 |
| 路由 | React Router | 6.x | SPA路由 |
| HTTP客户端 | Axios | 1.x | 请求封装 |
| WebSocket | socket.io-client | 4.x | 实时行情推送 |
| 图表库 | ECharts | 5.x | K线图、深度图 |
| 国际化 | i18next | 23.x | 多语言支持 |
| 样式方案 | CSS Modules + SCSS | - | 模块化样式 |
| 代码规范 | ESLint + Prettier | - | 代码质量 |

### 1.2 管理后台（Web - Vue3）

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 核心框架 | Vue | 3.x | UI框架 |
| 语言 | TypeScript | 5.x | 类型安全 |
| UI组件库 | Element Plus | 2.x | 企业级组件库 |
| 构建工具 | Vite | 5.x | 快速构建 |
| 状态管理 | Pinia | 2.x | Vue状态管理 |
| 路由 | Vue Router | 4.x | SPA路由 |
| HTTP客户端 | Axios | 1.x | 请求封装 |
| 图表库 | ECharts | 5.x | 数据可视化 |
| 国际化 | vue-i18n | 9.x | 多语言支持 |
| 样式方案 | SCSS | - | 样式预处理 |
| 表格 | Element Plus Table | - | 数据表格 |
| 表单 | Element Plus Form | - | 表单组件 |
| 代码规范 | ESLint + Prettier | - | 代码质量 |

### 1.3 移动端（Flutter）

| 类别 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 框架 | Flutter | 3.x | 跨平台框架 |
| 语言 | Dart | 3.x | 开发语言 |
| 状态管理 | flutter_bloc | 8.x | BLoC模式 |
| 路由 | go_router | 12.x | 声明式路由 |
| HTTP | dio | 5.x | 网络请求 |
| WebSocket | web_socket_channel | 2.x | 实时通信 |
| 本地存储 | shared_preferences | 2.x | 本地数据 |
| 图表 | fl_chart | 0.65.x | K线图 |
| UI组件 | Material Design 3 | - | 谷歌设计语言 |

## 2. 项目结构

### 2.1 React 交易终端结构

```
fx-trader-web/
├── public/                 # 静态资源
├── src/
│   ├── api/               # 接口请求模块
│   │   ├── quote.ts      # 行情接口
│   │   ├── order.ts      # 订单接口
│   │   ├── account.ts     # 账户接口
│   │   └── index.ts      # 统一导出
│   ├── assets/            # 静态资源
│   │   ├── images/        # 图片
│   │   └── styles/        # 全局样式
│   ├── components/         # 公共组件
│   │   ├── common/        # 通用组件
│   │   │   ├── PageLoading.tsx
│   │   │   └── ErrorBoundary.tsx
│   │   ├── chart/         # 图表组件
│   │   │   ├── CandlestickChart.tsx
│   │   │   └── DepthChart.tsx
│   │   ├── quote/         # 行情组件
│   │   │   ├── SymbolTicker.tsx
│   │   │   └── PriceBoard.tsx
│   │   └── trade/         # 交易组件
│   │       ├── OrderForm.tsx
│   │       └── PositionCard.tsx
│   ├── composables/       # 组合式函数
│   │   ├── useWebSocket.ts
│   │   ├── useOrder.ts
│   │   └── useAccount.ts
│   ├── hooks/             # 自定义Hook
│   ├── layouts/           # 布局组件
│   │   ├── MainLayout.tsx
│   │   └── TradeLayout.tsx
│   ├── pages/             # 页面组件
│   │   ├── trading/       # 交易页面
│   │   │   ├── TradingPage.tsx
│   │   │   └── OrderHistory.tsx
│   │   ├── account/       # 账户页面
│   │   │   └── AccountPage.tsx
│   │   └── report/        # 报表页面
│   │       └── ReportPage.tsx
│   ├── router/            # 路由配置
│   │   └── index.tsx
│   ├── store/             # 状态管理
│   │   ├── quoteStore.ts
│   │   ├── orderStore.ts
│   │   ├── accountStore.ts
│   │   └── userStore.ts
│   ├── types/             # TypeScript类型定义
│   │   ├── api.d.ts
│   │   ├── order.d.ts
│   │   └── quote.d.ts
│   ├── utils/             # 工具函数
│   │   ├── format.ts      # 格式化工具
│   │   ├── validation.ts  # 校验工具
│   │   └── storage.ts     # 存储工具
│   ├── App.tsx
│   ├── main.tsx
│   └── index.scss
├── .env.development       # 开发环境变量
├── .env.production        # 生产环境变量
├── vite.config.ts
├── tsconfig.json
├── package.json
└── README.md
```

### 2.2 Vue3 管理后台结构

```
fx-admin-web/
├── public/                 # 静态资源
├── src/
│   ├── api/               # 接口请求模块
│   │   ├── system/        # 系统管理
│   │   ├── order/         # 订单管理
│   │   ├── customer/      # 客户管理
│   │   ├── risk/          # 风控管理
│   │   └── index.ts       # 统一导出
│   ├── assets/            # 静态资源
│   │   ├── images/
│   │   └── styles/
│   ├── components/         # 公共组件
│   │   ├── common/        # 通用组件
│   │   │   ├── Pagination.vue
│   │   │   ├── SearchForm.vue
│   │   │   └── ConfirmDialog.vue
│   │   ├── form/          # 表单组件
│   │   │   ├── DictSelect.vue
│   │   │   └── DateRangePicker.vue
│   │   └── table/         # 表格组件
│   │       └── DataTable.vue
│   ├── composables/        # 组合式函数
│   │   ├── useTable.ts
│   │   ├── useDialog.ts
│   │   └── usePermission.ts
│   ├── directives/         # 自定义指令
│   │   └── permission.ts
│   ├── layouts/            # 布局组件
│   │   ├── index.vue
│   │   ├── Sidebar.vue
│   │   ├── Header.vue
│   │   └── TagsView.vue
│   ├── router/            # 路由配置
│   │   └── index.ts
│   ├── store/             # 状态管理
│   │   ├── user.ts
│   │   ├── permission.ts
│   │   └── settings.ts
│   ├── types/             # TypeScript类型定义
│   ├── utils/             # 工具函数
│   │   ├── validate.ts
│   │   ├── format.ts
│   │   └── auth.ts
│   ├── views/             # 页面组件
│   │   ├── dashboard/
│   │   ├── system/        # 系统管理
│   │   ├── order/         # 订单管理
│   │   ├── customer/      # 客户管理
│   │   ├── risk/          # 风控管理
│   │   ├── report/        # 报表管理
│   │   └── compliance/    # 合规管理
│   ├── App.vue
│   ├── main.ts
│   └── styles/
│       ├── variables.scss
│       └── common.scss
├── .env.development
├── .env.production
├── vite.config.ts
├── tsconfig.json
├── package.json
└── README.md
```

### 2.3 Flutter 移动端结构

```
fx_trader_app/
├── lib/
│   ├── api/               # 接口请求
│   │   ├── api_client.dart
│   │   ├── quote_api.dart
│   │   ├── order_api.dart
│   │   └── account_api.dart
│   ├── app/               # 应用配置
│   │   ├── app.dart
│   │   ├── routes.dart
│   │   └── theme.dart
│   ├── bloc/              # BLoC状态管理
│   │   ├── quote/
│   │   │   ├── quote_bloc.dart
│   │   │   ├── quote_event.dart
│   │   │   └── quote_state.dart
│   │   ├── order/
│   │   └── account/
│   ├── models/             # 数据模型
│   │   ├── quote_model.dart
│   │   ├── order_model.dart
│   │   └── account_model.dart
│   ├── repositories/       # 数据仓库
│   │   ├── quote_repository.dart
│   │   └── order_repository.dart
│   ├── screens/            # 页面
│   │   ├── trading/
│   │   │   ├── trading_screen.dart
│   │   │   └── chart_screen.dart
│   │   ├── account/
│   │   │   └── account_screen.dart
│   │   └── settings/
│   │       └── settings_screen.dart
│   ├── widgets/            # 组件
│   │   ├── chart/
│   │   │   └── candlestick_chart.dart
│   │   ├── order/
│   │   │   └── order_form.dart
│   │   └── common/
│   │       └── loading_indicator.dart
│   └── utils/              # 工具函数
│       ├── formatters.dart
│       └── validators.dart
├── pubspec.yaml
└── README.md
```

## 3. 请求封装规范

### 3.1 React/Axios 实例配置

```typescript
// api/client.ts
import axios from 'axios';
import { message } from 'antd';

const client = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  timeout: 30000,
});

// 请求拦截器
client.interceptors.request.use((config) => {
  const token = localStorage.getItem('access_token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  config.headers['X-Tenant-Id'] = localStorage.getItem('tenant_id');
  return config;
});

// 响应拦截器
client.interceptors.response.use(
  (response) => response.data,
  (error) => {
    const { code, message: msg } = error.response?.data || {};
    
    if (code === 40101) {
      // Token过期，跳转登录
      localStorage.clear();
      window.location.href = '/login';
    } else {
      message.error(msg || '请求失败');
    }
    return Promise.reject(error);
  }
);

export default client;
```

### 3.2 Flutter/Dio 实例配置

```dart
// api/api_client.dart
class ApiClient {
  static final Dio _dio = Dio(
    BaseOptions(
      baseUrl: EnvConfig.apiBaseUrl,
      connectTimeout: const Duration(seconds: 30),
      receiveTimeout: const Duration(seconds: 30),
      headers: {
        'Content-Type': 'application/json',
      },
    ),
  )..interceptors.add(
      InterceptorsWrapper(
        onRequest: (options, handler) {
          final token = StorageUtil.getToken();
          if (token != null) {
            options.headers['Authorization'] = 'Bearer $token';
          }
          return handler.next(options);
        },
        onError: (error, handler) {
          if (error.response?.statusCode == 401) {
            // Token过期处理
            Get.offAllNamed('/login');
          }
          return handler.next(error);
        },
      ),
    );
}
```

## 4. 路由规范

### 4.1 React 路由配置

```typescript
// router/index.tsx
const router = createBrowserRouter([
  {
    path: '/login',
    element: <LoginPage />,
  },
  {
    path: '/',
    element: <MainLayout />,
    children: [
      {
        path: '',
        element: <Navigate to="/trading" replace />,
      },
      {
        path: 'trading',
        element: <TradingPage />,
      },
      {
        path: 'positions',
        element: <PositionsPage />,
      },
      {
        path: 'orders',
        element: <OrdersPage />,
      },
      {
        path: 'account',
        element: <AccountPage />,
      },
    ],
  },
]);
```

### 4.2 Vue 路由配置

```typescript
// router/index.ts
export const constantRoutes: RouteRecordRaw[] = [
  {
    path: '/login',
    component: () => import('@/views/login/index.vue'),
    meta: { title: '登录', hidden: true }
  },
  {
    path: '/',
    component: () => import('@/layouts/index.vue'),
    redirect: '/dashboard',
    children: [
      {
        path: 'dashboard',
        component: () => import('@/views/dashboard/index.vue'),
        meta: { title: '工作台', icon: 'dashboard' }
      }
    ]
  }
];

export const asyncRoutes: RouteRecordRaw[] = [
  {
    path: '/order',
    component: () => import('@/layouts/index.vue'),
    meta: { title: '订单管理', icon: 'order', permission: 'order:view' },
    children: [
      {
        path: 'list',
        component: () => import('@/views/order/list.vue'),
        meta: { title: '订单列表', permission: 'order:view' }
      }
    ]
  }
];
```

## 5. 状态管理规范

### 5.1 React/Zustand Store

```typescript
// store/quoteStore.ts
import { create } from 'zustand';

interface QuoteState {
  symbols: Map<string, QuoteData>;
  subscribe: (symbols: string[]) => void;
  unsubscribe: (symbols: string[]) => void;
}

export const useQuoteStore = create<QuoteState>((set, get) => ({
  symbols: new Map(),
  
  subscribe: (symbols) => {
    // WebSocket订阅逻辑
    const ws = getWebSocket();
    ws.emit('subscribe', { symbols });
  },
  
  unsubscribe: (symbols) => {
    const ws = getWebSocket();
    ws.emit('unsubscribe', { symbols });
  },
}));
```

### 5.2 Vue/Pinia Store

```typescript
// store/user.ts
import { defineStore } from 'pinia';
import { getUserInfo, login, logout } from '@/api/system/user';

export const useUserStore = defineStore('user', {
  state: () => ({
    token: localStorage.getItem('token') || '',
    userId: '',
    username: '',
    roles: [],
    permissions: [],
  }),
  
  actions: {
    async login(userInfo: { username: string; password: string }) {
      const res = await login(userInfo);
      this.token = res.accessToken;
      localStorage.setItem('token', res.accessToken);
    },
    
    async getUserInfo() {
      const res = await getUserInfo();
      this.userId = res.userId;
      this.username = res.username;
      this.roles = res.roles;
      this.permissions = res.permissions;
    },
    
    async logout() {
      await logout();
      this.token = '';
      this.roles = [];
      this.permissions = [];
      localStorage.removeItem('token');
    },
  },
});
```

### 5.3 Flutter/BLoC

```dart
// bloc/order/order_bloc.dart
class OrderBloc extends Bloc<OrderEvent, OrderState> {
  final OrderRepository repository;
  
  OrderBloc({required this.repository}) : super(OrderInitial()) {
    on<CreateOrder>(_onCreateOrder);
    on<LoadOrders>(_onLoadOrders);
  }
  
  Future<void> _onCreateOrder(
    CreateOrder event,
    Emitter<OrderState> emit,
  ) async {
    emit(OrderLoading());
    try {
      final order = await repository.createOrder(event.params);
      emit(OrderCreated(order));
    } catch (e) {
      emit(OrderError(e.toString()));
    }
  }
}
```

## 6. 环境变量

### 6.1 React 环境变量

```env
# .env.development
VITE_API_BASE_URL=http://localhost:8080/api
VITE_WS_URL=ws://localhost:8080/ws
VITE_APP_TITLE=环球汇通(开发环境)

# .env.production
VITE_API_BASE_URL=https://api.globalfx.com/api
VITE_WS_URL=wss://api.globalfx.com/ws
VITE_APP_TITLE=环球汇通
```

### 6.2 Vue 环境变量

```env
# .env.development
VITE_API_BASE_URL=http://localhost:8081/api
VITE_APP_TITLE=环球汇通管理后台(开发)

# .env.production
VITE_API_BASE_URL=https://admin.globalfx.com/api
VITE_APP_TITLE=环球汇通管理后台
```

### 6.3 Flutter 环境变量

```dart
// lib/config/env_config.dart
class EnvConfig {
  static const String apiBaseUrl = String.fromEnvironment(
    'API_BASE_URL',
    defaultValue: 'https://api.globalfx.com',
  );
  
  static const String wsUrl = String.fromEnvironment(
    'WS_URL',
    defaultValue: 'wss://api.globalfx.com/ws',
  );
}
```

## 7. 代码规范

### 7.1 TypeScript 规范

- 启用严格模式
- 接口命名：PascalCase，如 `IUserInfo`、`OrderParams`
- 类型命名：PascalCase，如 `OrderStatus`
- 文件命名：kebab-case，如 `order-service.ts`
- 使用 `type` 定义简单类型，使用 `interface` 定义对象类型

### 7.2 组件规范

- 组件文件：PascalCase，如 `OrderForm.tsx`
- Props 接口：`XxxProps`
- 事件处理：`handleXxx`
- 业务逻辑：抽离到 hooks/composables

### 7.3 CSS/SCSS 规范

- 使用 CSS Modules 避免样式冲突
- 变量命名：kebab-case
- 颜色使用 CSS 变量，便于主题切换
- 响应式断点：mobile(768px)、tablet(1024px)、desktop(1280px)

## 8. 性能优化

### 8.1 React 优化

- 使用 `React.memo` 优化组件重渲染
- 使用 `useMemo` 和 `useCallback` 缓存计算结果
- 列表使用虚拟滚动（react-window）
- 图片懒加载
- 代码分割（React.lazy + Suspense）

### 8.2 Vue 优化

- 使用 `v-once` 优化静态内容
- 使用 `keep-alive` 缓存组件
- 路由懒加载
- 图片懒加载
- 组件按需引入

### 8.3 Flutter 优化

- 使用 `const` 构造不可变组件
- 列表使用 `ListView.builder`
- 图片使用缓存（cached_network_image）
- 代码压缩（--release）
